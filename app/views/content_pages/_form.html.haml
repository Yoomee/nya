=semantic_form_for(@content_page) do |form|
  =form.inputs do
    =form.input :title
    =form.input :subtitle
    =form.input :text, input_html: { class: 'redactor' }
    =form.input :slug, label: 'Url'
  =form.actions do
    =form.action :submit, label: 'Save'
