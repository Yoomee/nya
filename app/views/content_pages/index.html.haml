%h1
  Pages
  %small=link_to 'Add a page', new_content_page_path, icon: 'plus', class: 'ml-2'

%table.table.table-striped
  %tbody
    -ContentPage.all.each do |page|
      %tr
        %td=link_to page, "/#{page.slug}"
        %td=link_to 'edit', edit_content_page_path(page), class: 'btn btn-primary'
        %td=link_to 'delete', content_page_path(page), method: :delete, confirm: 'Are you sure?', class: 'btn btn-primary'
