=semantic_form_for(@project_category) do |form|
  =form.inputs do
    =form.input :name
    =form.input :slug, label: 'URL'
    =form.input :description
  =form.actions do
    =form.action :submit, label: 'Save'
